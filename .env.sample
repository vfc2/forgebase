# Moved: Environment variables now live under backend/.env and backend/.env.sample
# This file is kept only as a pointer to avoid confusion after the repo reorg.
#
# Please copy and edit backend/.env.sample instead:
#
#   cd backend
#   cp .env.sample .env
#   # then fill in AZURE_* values if using Azure OpenAI
